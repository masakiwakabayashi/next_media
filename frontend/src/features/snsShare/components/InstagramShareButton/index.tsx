import InstagramIcon from '@mui/icons-material/Instagram';
import { Button } from '@mui/material';

export const InstagramShareButton = () => {
  return (
    <Button variant="contained">
      <InstagramIcon />
    </Button>
  );
}
